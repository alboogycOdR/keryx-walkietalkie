# Handover — 2026-08-22

## State: GREEN
Builds, tests pass (1021/0/40), all work committed. Two builders active, one critical path task in progress.

## What changed this session
- **TASK-032** (S5): mesh transport injection seam — `MeshController` now accepts optional `MeshFloorTransport?` parameter, enabling host code to compose a single transport instance with both `FloorEngine` and `MeshController`. Verified twice with mutation testing (delete-the-API caused compilation failure, accept-but-ignore-the-param caused composed-cycle test failure). Merged.

- **TASK-033** (GB): real `DeviceAudioSink` over `flutter_soloud` + controlled pubspec unfreeze. Repo now makes actual sound. Two deps added exactly (flutter_soloud, permission_handler), no others. Independent build reproduced all claims except APK size (GB claimed 211MB, got 152MB independently; build succeeded both ways, cache-state dependent). Merged.

- **TASK-034** (CX): SFX projection double-play bug fixed. Rework: removed redundant overlapping subscriptions (DenyBuzz played twice — once from floor effect, once from state transition), added regression test that combines both stream sources and proves no double-play. Merged.

- **TASK-036** (CX): relay/token URL config + mode picker. `mode` field (persisted since TASK-030, rendered by nothing) is now live. Scheme validation per-field: relayUrl accepts only `wss`, tokenServiceUrl only `https`. Mutation-tested scheme check. Merged.

- **TASK-040** (GB, first-pass + rework): relay/token-svc validation + runbook foundation. 
  - Real end-to-end join proof: garbage JWT → HTTP 401; minted JWT → HTTP 101 + participant listed in RoomService.
  - `LIVEKIT_KEYS` space bug found and empirically confirmed: LiveKit v1.9.11 exits `Could not parse keys, it needs to be exactly, "key: secret", including the space` without it.
  - Caddy `/token` route wired from 503 placeholder to reverse-proxy at `{$TOKEN_SVC_UPSTREAM}`.
  - `ops/TWO_PHONE_TEST.md` created (relay-side sections only; TASK-039 appends app-side). 12-row symptom→cause→logs triage table, TS §8.7 privacy constraints, DNS/TLS honesty.
  - First-pass defect: validate.ps1/.sh exit 1 (caller/callee drift — GB's clobber fix redirected output to $TEMP but render_config.py still called `relative_to()` on external dest). Rework: fallback `display_write_path()` helper + new `test_validate_gate.py` test that executes the gate (proving the test suite itself was blind to the failure). Merged.

- **PLAN.md v9.3**: integrated-wave status updated. 37 of 39 tasks done/merged (95%).

## Half-done / in-flight
**TASK-035 (S5, in_progress)**: session/host-composition layer — the single hardest task of the integration wave. Both its dependencies (TASK-032, TASK-036) are done. This unblocks TASK-037 (face), which then unblocks TASK-038 (permissions) and TASK-039 (release APK).

Expected scope: compose LiveKit session controller (handles LINKED relay init + JWT minting), session state projection (the flow from session state into RadioState reducer), and a `SessionProvider` so face screen can inject the session into its widget tree. No production code yet; awaiting S5 submission.

## Next 3 steps — concrete, in order
1. **S5 finishes TASK-035 and lands on master.** This unblocks 3 downstream tasks on 2 builders.
2. **S5 claims TASK-037** (face integration: use the real transports, hook up roster discovery, surface SFX + engine events on screen). Depends on TASK-035 + TASK-033 (AudioSink) + TASK-034 (SFX projection) — all done.
3. **S5 claims TASK-038** (permissions + foreground service, same territory as TASK-037) after TASK-037 merges. Then GB can claim TASK-039 (release APK + app-side field script appended to the runbook created by TASK-040).

The two-phone test gates close when TASK-038 lands.

## Traps discovered
- **Concurrent builder writes to shared PLAN.md.** TASK-032 resume encountered a garbled, uncommitted diff in the shared main-checkout PLAN.md (looked like concurrent builder claim writes landing in wrong blocks). S5 correctly discarded it via `git checkout -- PLAN.md` per protocol. This is a known-risk (§10b "don't build on uncommitted shared state") and `plan_commit.sh` hardening remains a candidate if 3-way dispatch becomes routine.

- **Dispatch checks `Depends_On` only at assignment time.** ORCH dispatched GB against TASK-039 while its `Depends_On: TASK-037` was unmet. GB correctly refused per protocol and recommended the fix (split TASK-039 into TASK-040 for relay-side work with no dependencies). Standing lesson: validate `Depends_On` at dispatch time too.

- **APK size cannot be cached as a baseline.** TASK-033's debug-APK dossier cited 211MB; ORCH's independent build produced 152MB. Both succeeded, likely Gradle cache state variance. TASK-039's release build should measure its own APK fresh rather than treating 211MB as a regression threshold.

## Don't touch
- `specs/KERYX_World_Band_Radio_Spec_v1.0.md` — Phase 2 (world-band radio), explicitly out of scope for Phase 1 integration wave. Do not fold into decompose passes without being asked.

- `backups/`, `board/` — session artifacts, not part of repo.

- Parked FR-025 (emergency-preemption double-grant) — 40 soak seeds named and explicitly PARKED by project-owner decision 2026-08-21T17:05Z. Not a regression; structurally separate from the TASK-031 double-grant fix. Do not weaken SafetyMonitor or churn model to make these pass.

## Traps to add to project LESSONS
None new this session that aren't already in the global Flutter LESSONS.md. (The `LIVEKIT_KEYS` space bug is LiveKit-specific, not Flutter-transferable.)
