# TASK-072 — UX R2 tokens — amber accent, PTT ring/face tokens, tab indicator, golden refresh

## Brief

The owner chose amber/radio-yellow as the accent (ADR-002 O3). This task retunes `actionPrimary`, adds the tokens the new PTT ring and tab strip need, and regenerates every golden so the repaint lands in one reviewed step before any screen work.

## Spec pointers

- docs/adr/ADR-002-zello-aligned-talk-first-ui.md §3 A5, A3, A1
- specs/KERYX_Mobile_UX_Redesign_Design_v1.0.md §3.2 (tokens, contrast), §3.4
- lib/core/theme/ux_tokens.dart — existing palette + `contrastingOn`
- Current `stateWarning` is already amber (#F0B44C dark): after this change it must never be used on the PTT ring (A5)

## Approach

1. Pick amber values for dark and light, check contrast with the existing helpers, and make sure `contrastingOn(accent)` resolves to a dark foreground in dark mode.
2. Add the new tokens without renaming anything; dartdoc each with its ADR-002 clause.
3. Add hue-separation and contrast tests.
4. Run `flutter test --update-goldens test/regression/goldens`, spot-check the PNGs, and list them in the Work Log.

## Work Log

- [2026-09-11T01:55:00Z] [S5] Implemented. `actionPrimary` retuned to amber/radio-yellow: dark `#FFD54F` (hue 45.7°), light `#8A6D00` (hue 47.4°) — both computed to clear the graphical 3:1 floor against all three surfaces in their theme with margin, and hue-separated from `stateEmergency` by ≥20° (dark: 45.7° vs 24.6° = 21.1°; light: 47.4° vs 24.4° = 23.0°) — verified by a dedicated test (`keryxHueDelta`), not just eyeballed. Added `keryxHueDegrees`/`keryxHueDelta` helpers (HSL-based) to `ux_tokens.dart`. Added new tokens without renaming/removing any existing one: `KeryxUxPalette.pttFace` (single dark disc-face colour, identical in both themes, luminance < 0.05), `KeryxUxPalette.pttNeutralRing` (per-theme neutral gray, distinct from every `state/*` token and from the new accent), and a new `abstract final class KeryxUxPttTokens` holding `ringWidthFraction=0.08`, `widthFraction=0.78`, `maxDiameter=300`, `tabIndicatorThickness=3`, each mirrored as a passthrough getter on `KeryxUxTokens` (`pttFace`, `pttNeutralRing`, `pttRingWidthFraction`, `pttWidthFraction`, `pttMaxDiameter`, `tabIndicatorThickness`). `KeryxUxPalette.lerp` extended to interpolate the two new colour fields. Updated `specBodyTextMisses`: dark `action/primary` vs `surface/raised` was a documented miss (4.40) before the retune and now clears 4.5:1 with margin (9.97) with the new amber, so it was removed from the list and the dartdoc's "minimal corrections" comment updated; light `action/primary` vs `surface/raised` is still a miss, value updated from 4.45 to the newly-measured 4.19; `state/tx`/`state/rx` misses (unrelated tokens, untouched) kept as-is. Test file updated in lockstep: dark/light verbatim-column assertions now expect the new hexes; the "records measured ratios" test drops the now-passing dark entry and updates the light one; added three new test groups — hue-separation (`stateEmergency` vs `actionPrimary` ≥20° both themes), `stateTx` red-family distinctness from the new accent, and a `pttNeutralRing`-vs-`stateWarning` regression guard — plus a "PTT ring / tab tokens" group asserting the new tokens' existence, values, theme passthrough, and `lerp` interpolation of the two new colour fields.
- [2026-09-11T02:05:00Z] [S5] Regenerated every golden under `test/regression/goldens/goldens/` via `flutter test --no-pub --update-goldens test/regression/goldens` (37/37 pass). 30 of 36 PNGs changed (list: `channels_empty_{dark,light}`, `channels_populated_{dark,light}`, `qr_export_{dark,light}`, `qr_scan_denied_{dark,light}`, `radio_controls_{dark,light}`, `settings_{dark,light}`, `stations_populated_{dark,light}`, `talk_degraded_{dark,light}`, `talk_emergency_{dark,light}`, `talk_granted_{dark,light}`, `talk_idle_{dark,light}`, `talk_permission_denied_{dark,light}`, `talk_receiving_{dark,light}`, `talk_requesting_{dark,light}`, `talk_service_fault_{dark,light}`); the other 6 (`qr_scan_granted_{dark,light}` and 4 more not touching `actionPrimary`) were regenerated byte-identical, i.e. no diff, because nothing on those screens reads the accent token. Visually spot-checked `talk_idle_dark.png` directly (Read tool, rendered image): PTT disc is now amber/radio-yellow, every other surface/text/border colour, layout, and glyph position unchanged from before — confirms the diff is colour-only, not a layout regression.
- [2026-09-11T02:10:00Z] [S5] Test evidence: `flutter analyze lib/core/theme/ux_tokens.dart test/core/theme/ux_tokens_test.dart` → No issues found. `flutter analyze --no-pub` (repo-wide) → No issues found. `flutter test --no-pub test/core/theme/ux_tokens_test.dart` → 24/24 pass (was 17, +7 new tests, 0 removed). `flutter test --no-pub` (full suite) → **1429 passed / 0 failed / 40 skipped**, exit 0 — same 40 PARKED FR-025 soak-seed skips as the pre-change baseline (1422 passed before this task's own +7 new unit tests = 1429; golden count unchanged at the suite level since these are the same test cases, only their golden files changed). `git diff --stat` — production/test files: `lib/core/theme/ux_tokens.dart` (+86/-13... net), `test/core/theme/ux_tokens_test.dart`; plus 30 regenerated golden PNGs; all inside `Owned_Paths`. → Status: needs_review.