# TASK-025 — Event QR generate/scan + keryx:// deep links (KRX-054)

## Brief
Event join flow in `lib/features/event_qr/`: export any channel as a QR + `keryx://` deep link (region, channel, code or keyed token, expiry with spec presets), and a scanner that tunes the radio instantly. The token payload format is shared with the token service's expiry enforcement — document it as a contract.

## Spec pointers
- FR-044: "any channel can be exported as a QR code + `keryx://` deep link encoding region, channel, code (or keyed-channel token), and expiry. **Default expiry: 24 h**, with presets (4 h 'session', 24 h, 7 d, no expiry — the last requiring an explicit extra tap). Expired tokens are refused by the token service… Scanning tunes the radio instantly."
- FR-043: Event QR is one of the three LINKED join methods.
- TS §4 Event Crew persona: "Coordinator prints a QR; 12 volunteers scan and land on CH 7 · code 21."
- Privacy: keyed-channel QR carries a token, never the raw passphrase in plaintext where avoidable — coordinate format with TASK-003's `EVENT_TOKEN_SECRET`-signed expiry token (read token-svc/README contract; ORCH reconciles if the halves drift).

## Intended approach
1. `event_link.dart`: `keryx://join?v=1&r=<region>&ch=<n>&pc=<code>&exp=<unix>` (numbered) or `keryx://join?v=1&k=<keyed-token>&exp=<unix>`; encode/decode + expiry validation client-side (expired scan → in-world display flag + tone, no dialog).
2. `qr_export_screen.dart`: in-world "print card" style QR (qr_flutter) with expiry preset picker — 4 h / 24 h (default) / 7 d / no-expiry behind an explicit second tap per FR-044.
3. `qr_scan_screen.dart`: mobile_scanner view; successful decode emits a `tuneDirect`/join intent immediately ("tunes the radio instantly") and pops back to the face.
4. Deep-link payload parser exported for the Android intent-filter consumer (manifest registration belongs to the android/** chain — not this task).
5. Tests: encode/decode round-trip, expiry presets incl. default 24 h, expired refusal, malformed links ignored.

## Work Log
- [2026-08-21T11:40:00Z] [S5] Claimed and implemented on `task/TASK-025-s5`. Preflight (c8b9872): 3 entries, `lib/features/event_qr/**` and `test/features/event_qr/**` both NEW territory, `dossiers/TASK-025.md` existing (this file). Worktree needed a `git merge --ff-only` of a stale local PLAN.md snapshot to clear a chicken-and-egg territory-firewall block on the claim commit itself (branch had been cut from an older master tip than the claim landed on) — PLAN.md-only, no code, documented in the PLAN.md Progress_Note.
  Design: `event_link.dart` is the wire format (`keryx://join?v=1&...`) — numbered channels re-derive `roomId` locally via `deriveNumbered` (cheap HMAC), keyed channels carry the already-derived `roomId` (never the passphrase, per TASK-007's own contract). `buildKeyedEventLink()` is the MANDATORY off-UI-isolate path (`compute()` + top-level entry point, mirroring `LinkedController._deriveKeyedOffUiIsolate` exactly) for exporting a keyed channel from a fresh passphrase. `qr_export_screen.dart` renders the QR + link + FR-044 expiry-preset picker (no-expiry needs an explicit second tap). `qr_scan_screen.dart` splits the camera widget (`EventQrScanScreen`, thin, MobileScanner-backed) from a pure/testable "handle exactly one scan" policy (`EventQrScanHandler` + `handleBarcodeCapture`) — the camera widget itself is not exercised under `flutter test` (no platform channel available), matching this project's standing convention for native/platform adapters (TASK-026 review precedent).
  Deliberate scope boundary, disclosed in the feature README rather than invented around: token-svc's separate `keryx-evt.v1` event_token signing contract (needs `EVENT_TOKEN_SECRET`) is NOT implemented here — no Dart-side config surface for that secret exists anywhere in `lib/**` (grep-confirmed), and adding one is a `lib/core/settings/**`-shaped concern outside this task's `Owned_Paths`. `LinkedController.joinRoomId`'s `eventToken` param stays available for whichever task ORCH assigns that follow-up to.
  Acceptance criteria: (1) export encodes region/channel/code or keyed roomId + expiry — done, tested. (2) 4h/24h(default)/7d/no-expiry presets, no-expiry gated behind an explicit second tap — done, tested (export-screen widget tests). (3) scan emits a tuning intent instantly via `EventQrScanHandler.onTuned` — done, tested; "first-class LINKED join method" satisfied structurally (this is the QR/link join path; live radio-state wiring is host integration, same boundary TASK-024 drew for its own out-of-scope wiring). (4) `deriveKeyed` off UI isolate — `compute()` + top-level fn (mechanism) AND a `Timer.periodic` liveness proof that ticks while the derivation is in flight (behaviour) — both asserted, matches the criterion's own two-part sound form exactly, not TASK-024's since-flagged-unsound one. (5) encode/decode/expiry unit tests green — 43 new tests, full suite 508/508 (was 465/465 merged), `flutter analyze` clean, static grep-count cross-check on all 3 test files matches the reporter's own total exactly (no reporter discrepancy).
  Test_Evidence and Progress_Notes recorded in PLAN.md; handing off to needs_review.
