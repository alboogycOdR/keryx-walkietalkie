# TASK-088 — v2 session and host — target switching, roster-at-start, automatic direct/relay transport, v2 state and settings model

## Brief

The engine-facing half of v2. `RadioState`: remove `channel`, `privacyCode`, `mode`; add `roomId`, `transport (none|direct|relay|both)`; `SetMode` → `SetTransport`; update the reducer and its transition-matrix oracle in lockstep. `KeryxSettings`: remove `mode/region/channel/privacyCode`; add `preferDirectOnWifi` (default true), `messageRetention` (default 7 d), keep the relay URL. `RadioSessionController`: `retune` → `switchTarget(TalkTarget)` which tears down and rebuilds the chain for the target's room, calls `FloorEngine.updateRoster(members)` immediately from the directory member list (closes the solo join-guard, Technical §1.1), and runs LAN mesh and relay together, with the mesh only attaching listeners discovered on the LAN. `KeryxRadioHost.start`: load identity → directory `me` → presence socket → current target (last used, else first group, else none) → session. `RadioViewState`: add `target` and `audience {canHear, reason}`; `ConnectionCondition` becomes `{transport, degraded}`; audience ready rule per V2-FR-041. Fold in the carried debt: throttle meter snapshots (TASK-079 e) and seed the denied-flash timer on remount (TASK-082 a). `lib/core/floor/**` is not in this task and must not change.

## Spec pointers

- specs/KERYX_v2.0_Technical_v1.0.md §1.1, §6.3, §6.4, §7 (settings_model, radio_state, radio_session_controller); PRD V2-FR-040..045; Verification V2-VT-021, 022 (projection half), 023, 024; TASK-079 (e), TASK-082 (a) carried debt
- Owned_Paths: lib/services/session/**, lib/services/mesh/**, lib/core/radio_host/**, lib/core/state/**, lib/core/settings/**, lib/core/presentation/**, test/services/session/**, test/services/mesh/**, test/core/radio_host/**, test/core/state/**, test/core/settings/**, test/core/presentation/**, dossiers/TASK-088.md
- Depends_On: TASK-086, TASK-087

## Work Log

## Work Log

- [2026-09-11T20:05:00Z] [S5] Claimed, read the full Technical spec (§1.1-§11) and current source (radio_state.dart, radio_session_controller.dart, settings_model.dart, radio_view_state.dart, connection_condition.dart, keryx_radio_host.dart, radio_host_contract.dart, session.dart, session_host.dart). Found a real, verified OWNERSHIP_CONFLICT: the literal field removal (RadioMode/channel/privacyCode/SetMode/TuneTo) and the RadioSessionController.retune -> switchTarget rename the task Description and Technical §7 call for cannot compile without touching a dozen+ consumer files entirely outside this task's Owned_Paths (lib/features/channels/**, channel_selector/**, event_qr_ui/**, face/**, settings/**, settings_panel/**, stations/**, talk/**, app_shell/shell_routes.dart, tuning/stepper_button.dart) plus the SessionHost/RadioSessionHostAdapter seam in lib/features/face/session_host.dart. Technical §10's own planning-dependency graph sequences session/host (item 5) before Talk/shell rewiring (items 9-10) before Deletions (item 11) — so this collision is structural, not a mistake in decomposition per se, just a sequencing gap. Set Status: blocked, Blocked_Reason: OWNERSHIP_CONFLICT in PLAN.md with exact file list and three resolution options for ORCH (widen Owned_Paths, re-scope additive-only, or sequence 088 with 091-093). Zero code commits made — working tree clean on task/TASK-088-s5, only the PLAN.md claim commit exists. Full file-by-file read notes are in this session's context, ready to resume immediately once ORCH decides a direction.
- [2026-09-12T06:20:00Z] [S5] Re-claimed the re-scoped (additive-only, §6a) version. Rebased the existing branch (one dossier-only commit) onto current master, confirmed no v1 field/method removal is in scope, and read Technical §6.3/§6.4/§7/§6a plus TASK-086 (directory/contacts/groups controllers) and TASK-087 (room derivation, `RoomPrefix`, `LinkedController.joinRoomId`) artifacts before writing code, since both are reused rather than duplicated.
- [2026-09-12T07:10:00Z] [S5] Implemented, file by file:
  - `lib/core/state/radio_state.dart`: `Transport { none, direct, relay, both }` enum; `roomId`/`transport` fields (both default to the v1-safe null/`none`); `SetTransport`/`SetRoom` events accepted in any powered phase (mirrors `SetMode`'s TASK-080 fix — session composition needs these to land before the reducer reaches idle). `copyWith`/`==`/`hashCode`/`toString` all extended additively. `lib/core/state/radio_state_bridge.dart`'s exhaustive `_projectFloorEvent` switch needed a no-op arm for the two new event types to keep compiling — the only other file this task's Dart-level exhaustiveness forced a touch on, and it's already inside `lib/core/state/**`.
  - `lib/core/settings/settings_model.dart`: `preferDirectOnWifi` (default `true`) and `messageRetentionDays` (default 7, clamped 1-365 on read) as new optional fields with `fromJson` defaults — a pre-v2 persisted blob round-trips unchanged.
  - `lib/core/presentation/talk_target.dart` (new): `TalkTargetKind`/`TalkTarget` (contact or group, minimal — id/name/roomId/memberPeerIds only, deliberately not importing `lib/core/contacts/**`/`lib/core/groups/**`, a different task's territory) and `PeerPresence`/`AudienceState` with the V2-VT-022 {online,offline,dnd,busy}×{contact,group} matrix: only `online` members count as reachable; a specific reason string names *why* nobody can hear (offline/DND/busy, phrased per-contact when the target is a single contact) when `canHear == 0`. An empty-member-list target (solo) always reads "Nobody is listening" regardless of the matrix, per V2-FR-044/V2-VT-021. Exported from `presentation.dart`'s barrel.
  - `lib/core/presentation/connection_condition.dart`: `transport` getter — a stopgap projection of the existing `effectiveRoute` onto the v2 `Transport` vocabulary (local->direct, linked->relay, unresolved->none); documented as never able to report `both` until TASK-093 wires real `RadioState.transport` through.
  - `lib/core/presentation/radio_view_state.dart`: `target`/`audience` fields, defaulting to `null`/`AudienceState.everyoneReachable` so every existing v1 `project()` call keeps its old behaviour untouched; `project()` gained optional `target`/`presenceByPeerId` parameters that compute `audience` via `AudienceState.compute`.
  - `lib/services/session/radio_session_controller.dart`: new `switchTarget(TalkTarget, {required List<String> memberPeerIds})` alongside (not replacing) `retune`. `_startLocal`/`_startLinked` gained an additive `roomIdOverride` parameter — when supplied, LOCAL uses `RoomPrefix.compute(roomId)` (TASK-087) instead of the legacy `ChannelHashPrefix`, and LINKED calls `LinkedController.joinRoomId` (already used by `joinEvent`, TASK-087) instead of `joinNumbered`; v1's `start()`/`retune()` never pass it, so their behaviour is byte-identical. After the chain is up, `switchTarget` calls `FloorEngine.updateRoster`/`RadioStateBridge.updateRoster` with the full member list (self + `memberPeerIds`) *before returning* — closing the solo join-guard for v2 callers per Technical §1.1 — then dispatches `SetRoom`/`SetTransport` directly (both reducer-accepted in any powered phase) and queues `SetMode` exactly as `start()` does. Deliberately does NOT populate `_peers`/`PeerSession` (LAN-signaling-specific bookkeeping with no v2 analogue); `_stations` is published directly using peerId as a placeholder callsign, documented in-line — resolving real display names is `lib/core/contacts/**`/`lib/core/groups/**` territory, a later task.
  - `lib/core/radio_host/radio_session_host_v2.dart` (new): `RadioSessionHostV2`, a standalone seam for "load identity -> directory `me`/presence -> current target -> `switchTarget`" (Technical §6.4), built from three injected async callbacks rather than concrete `DirectoryClient`/contacts/groups types — keeps this file independent of those other tasks' territory and independently testable. Not constructed or called anywhere in `keryx_radio_host.dart`; `KeryxRadioHost`'s v1 `start()` path is completely untouched (confirmed by re-running its existing tests unmodified).
  - Carried debt (TASK-079 e, TASK-082 a): **not closed**, disclosed rather than silently dropped. (e): the reviewer's own finding calls the current dedup-only throttle "acceptable... revisit if profiling shows up cost" — not a defect. I did implement a real 250ms time-based throttle on `_setMeterLevel`, ran the full owned-file test suite, and found it broke an existing, currently-passing, real-time-behaviour test (`test/services/session/radio_session_controller_test.dart`'s "measured value flows through at ~10 Hz" case, which asserts two distinct measured values 100ms apart) — landing it would have violated AC7's "zero removed/weakened tests". Reverted rather than either landing a regression or weakening that test to accommodate my own change. (a) is in `lib/features/talk/talk_screen.dart`, entirely outside this task's `Owned_Paths` — a real OWNERSHIP_CONFLICT for that specific carried-debt item alone, not the whole task; disclosed here and in PLAN.md rather than touched.
  - Verification, run fresh in the foreground and waited on to completion: `flutter analyze --no-pub` (full repo) -> **No issues found**. `flutter test --no-pub` (full repo) -> **1697 passed, 0 failed, 40 skipped** (same PARKED FR-025 soak seeds, unmodified; baseline before this task was 1673, net +24 new tests across `radio_state_test.dart`, `settings_repository_test.dart`, `talk_target_test.dart` (new), `connection_condition_test.dart`, `radio_view_state_test.dart`, `radio_session_controller_test.dart`, `radio_session_host_v2_test.dart` (new)). `git diff --stat -- . ':!PLAN.md'` -> 16 files, all inside `Owned_Paths`; the only non-additive lines in any pre-existing file are `radio_state_test.dart`'s two extended transition-matrix oracle totals (145/87 -> 159/89), required by AC5 when the oracle is extended with `SetTransport`/`SetRoom` rows (7 legal + 1 illegal each across 8 phases). Status: needs_review.
