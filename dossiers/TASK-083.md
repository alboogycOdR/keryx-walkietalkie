# TASK-083 — v2 identity core — Ed25519 key pair, peer ID from public key, 12-word recovery phrase, sealed-box helpers

## Brief

First v2.0 task; reopens frozen `pubspec.yaml` for this task only (add `cryptography` and a BIP-39 word list; pin versions; no other dependency changes). Replace the install-UUID identity with an Ed25519 key pair generated on first run and stored in `flutter_secure_storage` via the existing `IdentityStore` seam. `derivePeerId` keeps its output shape (base32(sha256(x))[:10]) but takes the public key; add `deriveShortCode` = chars [10:14] of the same digest. Add `RecoveryPhrase` (128-bit entropy → 12 English BIP-39 words; seed → key via HKDF-SHA256 info `keryx-id-v1`; restore = phrase → same key). Add request signing (`X-Keryx-Sig/Key/Ts` over sha256(method|path|body|ts)) and sealed-box helpers (Ed25519→X25519 conversion, seal to a public key / open with the private key) for group secrets. Keep `callsign.dart` and its validation; keep `IdentityRepository`'s public API so existing consumers compile. Migration: an existing install with a UUID but no key gets a key generated and keeps its callsign (Technical §8). Nothing in this task touches UI or the network.

## Spec pointers

- specs/KERYX_v2.0_Technical_v1.0.md §3 (keys, ID, phrase, signing), §7 (peer_id.dart re-point); specs/KERYX_v2.0_PRD_v1.0.md V2-FR-001..004; specs/KERYX_v2.0_Verification_v1.0.md V2-VT-001..004; Product Model D1, D9, D10
- Owned_Paths: pubspec.yaml, pubspec.lock, lib/core/identity/**, test/core/identity/**, dossiers/TASK-083.md
- Depends_On: —

## Work Log

- [2026-09-11T18:00:00Z] [S5] Claimed. Preflight (`python scripts/preflight_paths.py TASK-083`):
  ```
  [preflight] TASK-083 Owned_Paths inspected in C:/CLAUDECODE_TOOLSETS/wt-s5-walkietalkie-keryx
  [preflight] 5 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.
    FILE   pubspec.yaml  -> exists, 116 line(s), 4618 bytes
    FILE   pubspec.lock  -> exists, 882 line(s), 27081 bytes
    GLOB   lib/core/identity/**  -> 9 file(s): README.md, callsign.dart, collision.dart, identity.dart, identity_repository.dart, identity_store.dart, install_uuid.dart, peer_id.dart, rfc4648_base32.dart
    GLOB   test/core/identity/**  -> 5 file(s): callsign_test.dart, collision_test.dart, identity_repository_test.dart, memory_identity_store.dart, peer_id_test.dart
    FILE   dossiers/TASK-083.md  -> exists, 13 line(s), 1611 bytes
  ```
  Read Technical §3/§7/§8, PRD V2-FR-001..004, Verification V2-VT-001..004, and every existing identity file before writing anything.
  Added `cryptography: ^2.7.0` to `pubspec.yaml` (resolved to 2.9.0, pure Dart, only 1 new transitive dependency — `flutter pub get` clean). Pinned the canonical BIP-39 English wordlist (2048 words, from `bitcoin/bips`) verbatim into `lib/core/identity/bip39_wordlist.dart` rather than adding a `package:bip39` dependency, per the dossier's "either is acceptable" note — keeps this task's dependency footprint to one package.
  Implemented: `keys.dart` (`IdentityKeyPair.generate()/fromSeed()`, Ed25519 sign/verify, and the Ed25519→X25519 birational map — Edwards point decompression via `BigInt` field arithmetic, `modPow`/`modInverse` mod 2^255-19 — used by `sealed_box.dart`); `recovery_phrase.dart` (128-bit entropy ↔ 12 BIP-39 words with the standard 4-bit checksum; `deriveKeyPair()` via HKDF-SHA256(entropy, info `keryx-id-v1`) per Technical §3.2, *not* the standard PBKDF2 mnemonic-to-seed — the spec calls for HKDF directly over the entropy); `signing.dart` (`X-Keryx-Sig/Key/Ts` over `sha256(method|path|body|ts)`, `signRequest`/`verifySignedRequest` with a 120s window, Technical §3.3); `sealed_box.dart` (ephemeral X25519 ECDH + HKDF-SHA256 + AES-256-GCM — a KERYX-internal construction, not libsodium's `crypto_box_seal` wire format, since every sealer/opener is this Dart client and the server never opens a sealed box per Technical §4.1's `secret_enc` comment, so cross-language byte compatibility isn't required); `peer_id.dart` re-pointed at the public key (`derivePeerId`/`deriveShortCode` now take the 32-byte key) while still accepting a legacy UUID `String` — kept purely so `test/simulation/sim_peer.dart` (outside this task's territory, a floor-engine soak harness Technical §1 keeps as-is) keeps compiling and producing its original election-simulation peerIds; `identity_repository.dart`/`DeviceIdentity` gained the Ed25519 `keyPair`/`shortCode` (optional, so hand-built `DeviceIdentity`s in `test/core/radio_host/**`, `test/features/face/**`, `test/features/settings/**`, `test/regression/real_composition_test.dart` — none in this task's territory — keep compiling unchanged) while keeping `installUuid`/`peerId`/`callsign` exactly as before in shape; migration is automatic (`_loadOrCreateKeyPair` mints a key whenever none is stored yet, regardless of whether a v1 UUID/callsign already exist, so an existing install keeps its callsign and gains a key on first v2 run, Technical §8).
  `flutter analyze --no-pub` (`lib/core/identity/`, then repo-wide): No issues found both times. `flutter test --no-pub test/core/identity/`: **62/62 pass** (new: `keys_test.dart`, `recovery_phrase_test.dart`, `signing_test.dart`, `sealed_box_test.dart`, `bip39_wordlist_test.dart`; rewritten: `peer_id_test.dart`, `identity_repository_test.dart` — both now exercise the public-key-based derivation, plus a new explicit v1→v2 migration test). Full `flutter test --no-pub`: **1558 passed / 1 failed / 40 skipped** (same pre-existing PARKED FR-025 soak-seed skips, untouched). The 1 failure is `test/core/audio/device_audio_sink_test.dart`'s `pubspec unfreeze adds exactly flutter_soloud and permission_handler` — a frozen-dependency-list guard test outside this task's `Owned_Paths` (`test/core/audio/**`), which now correctly detects the `cryptography` addition this task's own Description explicitly authorizes ("reopens frozen `pubspec.yaml` for this task only"). Not fixed here (out of territory); flagged for ORCH to update that assertion's expected set (add `cryptography`) alongside the merge — the failure is the intended, single, expected side effect of the authorized pubspec reopen, not a regression. `git diff` against the pre-claim base is 17 files, all inside Owned_Paths.
  All 6 acceptance criteria verified against the new tests (10,000-key collision property test added for V2-FR-001/V2-VT-001; phrase round-trip + one-word-change + invalid-word rejection for V2-VT-002; signing accept/tamper/stale/wrong-key for V2-VT-004; seal/open round-trip + wrong-key rejection for Technical §5.3; existing consumers compile unchanged — verified via repo-wide `flutter analyze`; migration test added). → Status: needs_review.
