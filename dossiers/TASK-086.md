# TASK-086 — v2 directory client and local stores — signed REST client, presence socket, contacts and groups stores

## Brief

Client half of the directory. `lib/services/directory/`: a REST client that signs every call with TASK-083's helper, typed models generated by hand from `openapi-v2.yaml`, typed errors for every enumerated code, and a presence WebSocket client with reconnect/backoff, heartbeat, and a `Stream<PresenceUpdate>`. `lib/core/contacts/`: a SQLite-backed store (`sqflite` is NOT in pubspec and pubspec is frozen after TASK-083 — use `shared_preferences` JSON with a 500-entry cap and a typed repository so the storage can be swapped later), the request state machine (pending-in, pending-out, accepted, blocked, expired), and a `ContactsController` exposing `Stream<List<Contact>>` with presence merged in. `lib/core/groups/`: group store, sealed-secret handling via TASK-083's helpers, rotation handling (fetch my sealed copy on notice, replace, emit a `GroupKeyChanged` event), admin/member role model. No UI, no session wiring. Test everything against a fake HTTP/WS layer; include one contract test that loads `openapi-v2.yaml` and checks every path this client calls exists in it.

## Spec pointers

- specs/KERYX_v2.0_Technical_v1.0.md §4.2 (contract), §4.3 (presence), §6.1 (modules); token-svc/openapi-v2.yaml (TASK-084's contract); PRD V2-FR-010..014, V2-FR-020..025, V2-FR-030..033; Verification V2-VT-013 (client side), V2-VT-025/026 (store behaviour)
- Owned_Paths: lib/services/directory/**, lib/core/contacts/**, lib/core/groups/**, test/services/directory/**, test/core/contacts/**, test/core/groups/**, dossiers/TASK-086.md
- Depends_On: TASK-083, TASK-084

## Work Log

- [2026-09-11T22:00:00Z] [S5] Resumed (this task was claimed by an S5 session and left with no code; a second concurrent S5 session collided writing the same files ~51min in and stood down when it saw active work — replied via SendMessage confirming I own this through to needs_review). Implemented on branch `task/TASK-086-s5`:
  - **`lib/services/directory/`**: `directory_signing.dart` (wraps TASK-083's `signRequest`, re-encodes `X-Keryx-Key` as unpadded base64url per the AC — the only header this task's own criterion asks to diverge from TASK-083's helper, done without touching `lib/core/identity/**`); `directory_errors.dart` (`DirectoryErrorCode` enum mirroring every code in `token-svc/app/errors.py`, `DirectoryException` typed on top of it, `.unknown` for forward-compat); `directory_models.dart` (hand-written `Me`/contact/group wire shapes); `directory_client.dart` (`dart:io` `HttpClient`, same convention as `TokenClient`; one method per `openapi-v2.yaml` operation this task needs — identity, contacts, groups, alerts; `DirectoryClient.calledPaths` is the list the contract test checks against the spec); `presence_transport.dart` (`PresenceTransport`/`PresenceSocket` abstraction — production impl wraps `dart:io` `WebSocket.connect` with the signed headers as the upgrade request's headers; a fake implements the same interface for tests, mirroring `LiveKitAdapter`'s split); `presence_client.dart` (connect/heartbeat-every-60s/reconnect-with-backoff/give-up-after-maxAttempts, `Stream<PresenceUpdate>`/`Stream<GroupRotationNotice>`/`Stream<AlertNotice>`, `setStatus` with a before-connect queue).
  - **`lib/core/contacts/`**: `contact_models.dart` (`Contact`, `PendingContactRequest` with `ContactRequestDirection`, `BlockedContact`); `contacts_repository.dart` (`shared_preferences` JSON, `contactsCap = 500`, oldest-dropped-first — `sqflite` is not in the frozen `pubspec.yaml`, per the task's own instruction); `contacts_controller.dart` (the request state machine: `sendRequest`/`accept`/`decline`/`block`/`removeContact`, `refreshFromServer` reconciling the local store to `GET /v2/identity/me`'s exact contents — which is also how a request the *other* side resolved disappears locally without a push — `pruneExpired` for 7-day expiry with an injectable clock, presence-update merging into the in-memory contact list).
  - **`lib/core/groups/`**: `group_models.dart` (`GroupMembership` carrying the *opened* secret + its derived `roomId`, `GroupMemberInfo`); `group_events.dart` (`GroupKeyChanged`, `GroupMembershipEnded`); `groups_repository.dart` (same `shared_preferences` convention); `groups_controller.dart` (`createGroup` mints a secret via `Random.secure()`, seals it to itself with TASK-083's `sealToPublicKey`, derives the room id with TASK-087's `deriveGroupRoom`; `joinGroup` seals the invite-carried secret the same way; `refreshFromServer` reconciles against `GET /v2/identity/me` — opens each group's sealed copy with `openSealed`, adopts on a `key_version` bump (emits `GroupKeyChanged`), and drops any group no longer listed (emits `GroupMembershipEnded` — this is how a *removed* member's store notices removal, since the contract has no explicit "you were removed" push, Technical §5.3's own words); `rotate`/`removeMember` accept a pre-sealed `secrets_enc` map from the caller (this store doesn't own sealing-to-every-member, since it doesn't have every member's public key — that's the UI/roster layer's job) and adopt the new secret locally immediately, so the acting admin doesn't wait for its own presence-WS notice; a `PresenceClient.rotationNotices` subscription triggers a full `refreshFromServer` on notice, since the contract's only way to fetch a fresh sealed copy is `GET /v2/identity/me` — there is no per-group "my sealed copy" endpoint).
  - Test fakes: `test/services/directory/fakes/fake_directory_server.dart` (a real loopback `HttpServer`, same convention as `fake_token_server.dart` — no HTTP mocking package in this repo); `fake_presence_transport.dart` (in-memory `PresenceTransport`, mirrors `fake_livekit_adapter.dart`).
  - Contract test: `test/services/directory/openapi_contract_test.dart` scans `token-svc/openapi-v2.yaml`'s `paths:` keys and asserts every entry in `DirectoryClient.calledPaths` is one of them.
  - One deliberate scope line: sealing a rotated/removed-member secret to every *other* member's public key is NOT done inside `GroupsController` — it doesn't have a roster of member public keys to seal to (that's `GET /v2/groups/{id}`'s member list, a UI-facing read this task doesn't own the screen for). `rotate`/`removeMember` take the already-sealed map as a parameter; the caller (a later UI/roster task) is expected to fetch the member list, seal to each, and pass the map in. Documented in the method dartdocs.
  - `flutter analyze --no-pub` (full repo): No issues found.
  - Targeted: `test/services/directory/` 18/18, `test/core/contacts/` 11/11, `test/core/groups/` 7/7 — all pass.
  - Full `flutter test --no-pub`: **1620 passed, 0 failed, 40 skipped** (same PARKED FR-025 soak-seed skips; was 1584 baseline + 36 net new).
  - `flutter build apk --debug`: Built `build/app/outputs/flutter-apk/app-debug.apk` successfully.
  → Status: needs_review.
