# TASK-023 — Floor-control simulation soak harness (KRX-044)

## Brief
A randomized multi-peer simulation suite in `test/simulation/` that soaks the TASK-022 floor engine over a simulated lossy, reordering, churning transport for 500 seeded runs and asserts the protocol's safety and timing invariants. This is the release-confidence instrument for floor control.

## Spec pointers
- TS §11 KRX-044: "Simulation test harness: 500-run randomized churn/loss soak, zero double-grants; asserts the locked timing constants and peerId election properties of §8.6."
- TS §8.6 timing table (presence 5 s/3 miss, lease TOT+2 s, re-election ≤ 500 ms, retry 150 ms ×3, debounce 750 ms) and peer identity ("uniformly distributed ID space that avoids election thrashing on simultaneous joins").
- NFR-10: "floor protocol simulation suite" is part of the coverage bar.

## Intended approach
1. `sim_transport.dart`: virtual network — per-link delay jitter, loss %, reordering, partition events; fake clock driving everything (no real timers → fast runs).
2. `sim_peer.dart`: N floor engines (TASK-022, read-only import) with generated peerIds (TASK-009 derivation, read-only import), random PTT press/release schedules, random join/leave churn, occasional crash (silent disappearance mid-TX).
3. `soak_test.dart`: 500 runs × randomized seed (seed printed on failure); invariants asserted per run: (a) at no simulated instant do two peers believe they hold the floor; (b) after any churn, a sole arbiter exists within 500 ms; (c) a crashed speaker's floor frees by lease end; (d) elections always pick the lexicographic minimum present; (e) retry/give-up counts match 150 ms ×3.
4. Runtime budget: keep the whole soak under ~2 min in CI (tune peer count ~4–8, run length ~120 simulated seconds).

## Work Log

- [2026-08-21T18:05:00Z] [S5] RESUMED (per ORCH's 2026-08-21T17:20:00Z Progress_Note). c8b9872 preflight re-run: `python scripts/preflight_paths.py TASK-023` → GLOB test/simulation/** -> 4 file(s) (safety_monitor.dart, sim_network.dart, sim_peer.dart, soak_test.dart); FILE dossiers/TASK-023.md -> exists, 17 lines. Branch `task/TASK-023-s5` was still based on pre-TASK-031 master (merge-base `434bf20`); merged current master (`f50f87d`, includes TASK-031's `f3109ee` fix + unrelated TASK-018/025/026/030 wave work) into the branch — clean merge, no conflicts, no file under `lib/` touched by me. Ran `flutter test test/simulation` against the merged fix: 464/505 pass, 41 fail — the deterministic "KNOWN ISSUE" test (now fails for the OPPOSITE reason, as expected — `a` is correctly BUSY-denied instead of double-granting) plus exactly 40 soak seeds (21,22,34,39,65,78,85,108,116,118,136,138,140,146,153,167,183,188,190,198,213,219,236,251,260,263,266,296,308,312,333,344,359,360,449,450,459,469,471,497), matching the ~40/500 FR-025 figure ORCH independently traced during TASK-031's review.
- [2026-08-21T18:20:00Z] [S5] Rewrote the "KNOWN ISSUE" group into a permanent regression guard ("REGRESSION GUARD (TASK-031 fix, was 'KNOWN ISSUE' pre-fix)"): same two-peer setup (b solo-grants, a joins with a lower peerId and out-ranks b), but now asserts `a.engine.isTransmitting` is `false` (BUSY-denied by the join-guard window) and `b.engine.isTransmitting` stays `true` — locking in TASK-031's fix rather than the pre-fix double-grant shape. Did NOT weaken `SafetyMonitor` or any invariant — only this one test's assertions changed, per the task's explicit instruction. Verified it fails if reverted to the old double-grant assertions (sanity-checked mentally against the pre-merge run, which showed this exact test failing with `a.isTransmitting == true`).
- [2026-08-21T18:25:00Z] [S5] Added a named `_parkedFr025Seeds` set (the exact 40 seeds above) plus a `_parkedFr025SkipReason` constant, and passed `skip:` on the per-seed soak test — each parked seed now shows as an explicit, reasoned `[skip]` in test output rather than silently passing or being deleted. Did not touch `SafetyMonitor`'s invariant or the churn model. `flutter analyze`: No issues found. `flutter test test/simulation`: 465 passed, 40 skipped (all named FR-025, reason printed), 0 failed — "All tests passed!". Full unfiltered `flutter test`: 995 passed, 40 skipped, 0 failed.
- [2026-08-21T18:30:00Z] [S5] Committed to `task/TASK-023-s5` (`e907c11`). Did not attempt a minimal deterministic repro for the FR-025 class (nice-to-have per the task description, not a blocker) — the existing per-seed identification (exact seed list + stack trace class already on record in PLAN.md from TASK-031's review) already gives a reproducible pointer (`_runOnce(<seed>)` for any of the 40), and isolating a fresh 3-line repro the way the join-guard defect was isolated risks scope creep toward actually diagnosing/fixing FR-025, which is explicitly out of scope. → `needs_review`.