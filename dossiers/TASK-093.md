# TASK-093 — v2 shell and settings — Talk/Contacts/Groups tabs, My code in overflow, onboarding and restore routes, v2 settings sections

## Brief

Rewire the shell for v2: tabs Talk (mic) / Contacts (person) / Groups (groups); ⋮ gains My code above Radio controls and Settings; first launch routes to onboarding before the shell; a 'Restore' entry on the callsign screen. Target selection from Contacts/Groups switches to Talk via `switchTarget`. Settings: replace the Connectivity section with relay address + prefer-direct; add Identity (callsign, Show recovery phrase behind device lock, Restore) and Messages (retention, marked 'used from v2.1'); remove the mode/region rows. Keep the TASK-077 back-button rules and add the new tabs to the real-back tests. Delete the debug legacy-face route registration from `app.dart`. Do not delete feature directories (TASK-094).

## Spec pointers

- specs/KERYX_v2.0_Design_v1.0.md §1, §2.7; Technical §6.4 (host start), §8 (v1 install migration); PRD V2-FR-060/061; Verification V2-VT-027 (shell half), V2-VT-029, V2-VT-030 (shell frame goldens)
- Owned_Paths: lib/app_shell/**, lib/app.dart, lib/main.dart, lib/features/settings/**, test/app_shell/**, test/features/settings/**, test/regression/real_composition_test.dart, test/regression/goldens/goldens/shell_frame_*.png, test/regression/goldens/goldens/settings_*.png, test/regression/goldens/shell_frame_golden_test.dart, test/regression/goldens/settings_golden_test.dart, dossiers/TASK-093.md
- Depends_On: TASK-089, TASK-090, TASK-091, TASK-092

## Work Log

- [2026-09-12] [S5] Claimed. Resume check: session-start checkpoint pointed at TASK-092, which was already reviewed/merged/`done` at `961b76f` — stale, deleted. No S5 in_progress/claimed task existed. Depends_On TASK-089/090/091/092 all `done`. Preflight (c8b9872 filesystem check, pasted at claim time in PLAN.md) confirmed all Owned_Paths exist. Branch `task/TASK-093-s5` off master `7b6adff`.

- [2026-09-12] [S5] **Production code complete and `flutter analyze --no-pub lib/` clean.** Test suite is NOT yet updated for the new shell shape — see "Known gaps" below. Status stays `in_progress`; this is not ready for `needs_review`.

  Implemented, all within `Owned_Paths`:
  - `lib/app_shell/directory_providers.dart` (new): the directory/contacts/groups composition root — `identityProvider`, `directoryClientProvider`/`presenceClientProvider`/`contactsControllerProvider`/`groupsControllerProvider` (all `null` when no relay is configured, so Contacts/Groups render a real empty state rather than crashing), `currentTargetProvider` (presentation-only current `TalkTarget`), `presenceByPeerIdProvider` (flat map built from `PresenceClient.updates`). **Disclosed decision:** there is no `directoryUrl` field on `KeryxSettings` (frozen) — `directoryBaseUri()` derives an HTTPS origin from `relayUrl`, mirroring `KeryxSettings.resolvedTokenServiceUrl`'s own precedent. Flagged for ORCH/a successor task if that assumption is wrong.
  - `lib/app_shell/contacts_tab_screen.dart` (new): wraps TASK-090's `ContactsTab` over a real `ContactsController`; row tap decodes the contact's unpadded-base64url public key, derives the 1:1 room via `deriveDirectRoom` (TASK-087), and calls `onSelectTarget`.
  - `lib/app_shell/groups_tab_screen.dart` (new): wraps TASK-091's `GroupsListScreen` + owns list→detail→invite/new-group/join-with-code navigation (that feature's screens are standalone with injected callbacks, per that task's own note — nothing there wires navigation itself). Row tap builds a group `TalkTarget` from the cached `GroupMembership.roomId`. **Known gap:** `GroupListRow.onlineCount` is hardcoded to 0/1 rather than a real presence-derived count — the per-group member/presence fetch (`GroupsListController`, TASK-091's own file) wasn't wired in given remaining time; the rows render but the online count is not live.
  - `lib/app_shell/onboarding_gate.dart` (new): fresh install (no `IdentityRepository.privateKeySeedKey` stored) shows a create/restore chooser; create generates a `RecoveryPhrase`, mounts TASK-089's `OnboardingScreen`, and on done persists the phrase-derived key + callsign + (in a dedicated secure-storage key this file owns) the phrase words themselves, for Settings' "Show recovery phrase" to read back later — `IdentityRepository` never stores the phrase (HKDF is one-way) and is frozen territory, so this is new state under this task's own code, not a modification of frozen code. Restore mounts TASK-089's `RestoreScreen` and persists its result the same way. A keyed install (including a v1 install `loadOrCreate()` already migrated) skips straight to `MobileAppShell`. **Disclosed compat decision:** Design's "'Restore' entry on the callsign screen" is surfaced here, at the pre-shell gate, instead — `OnboardingScreen`'s constructor (TASK-089's `Owned_Paths`) has no restore hook to attach one to.
  - `lib/app_shell/mobile_app_shell.dart`: tabs are now Talk/Contacts/Groups (glyphs mic/person/groups); ⋮ overflow gains "My code" above Radio controls/Settings; deleted the old shell-local `channels_screen.dart`/`stations_screen.dart` wrapper widgets (the underlying `lib/features/channels/**`/`lib/features/stations/**` directories are untouched — TASK-094's job). Selecting a contact/group anywhere sets `currentTargetProvider` and switches to the Talk tab (Design §1 "Current target").
  - `lib/app_shell/talk_screen.dart`: now passes `target`/`presenceByPeerId` from the new providers into TASK-092's `TalkScreen`, and wires `onAddContact`/`onCreateGroup` to tab switches. `onOpenPicker`/`onOpenStations` are simply omitted now (v2 has no numbered-channel picker or Stations tab).
  - `lib/app_shell/shell_routes.dart`: added `openMyCode` (Design §2.4), reading the real identity via `identityProvider`.
  - `lib/app.dart`: **deleted the debug legacy-face route registration** (this task's explicit instruction) and its `FaceScreen`/`kDebugMode` imports; `home` is now `OnboardingGate` instead of `MobileAppShell` directly.
  - `lib/features/settings/settings_screen.dart` + `settings_copy.dart` + `settings_keys.dart` + `settings_rows.dart` (new `SettingsActionRow`): removed the Radio mode and Region rows; added a "Prefer direct on Wi-Fi" toggle to Connectivity (`KeryxSettings.preferDirectOnWifi`, already existed — a TASK-088 additive field); added a read-only Messages section (`messageRetentionDays`, labelled "used from v2.1" per Design §2.7); added Identity's "Show recovery phrase" (confirmation dialog → `RecoveryPhraseViewScreen`, a new 3×4 mono grid screen, no copy affordance) and "Restore from phrase" (confirmation → `RestoreScreen` → persists via `IdentityRepository`, shows a "restart KERYX" SnackBar since a live mid-session identity hot-swap through the running `RadioHost`/directory session wasn't wired given remaining time). **Disclosed decision:** "device lock" gating is a same-device confirmation dialog, not biometric/PIN — `local_auth` isn't a dependency and adding one is a `pubspec.yaml` edit (frozen territory).
  - Deleted `lib/app_shell/legacy_compat.dart` (its only registration is gone; TASK-061/094 area) and its test.

  Verification so far: `flutter analyze --no-pub lib/` → **No issues found**. `flutter analyze --no-pub lib/features/settings/` and the three new `lib/app_shell/*.dart` files individually → clean throughout implementation.

  **Known gaps / debt, all disclosed rather than hidden:**
  1. **`RadioSessionController.switchTarget` is not wired to a live session.** `currentTargetProvider` only drives Talk's presentation (header card, audience computation, ready-ring rule) — it does not actually rejoin a different LiveKit room. `RadioSessionHostV2` (`lib/core/radio_host/radio_session_host_v2.dart`, Technical §6.4's named seam) needs a live `RadioSessionController` instance, and `KeryxRadioHost` (`lib/core/radio_host/keryx_radio_host.dart`, frozen territory) has no public accessor for the one it constructs per-session. This is an `OWNERSHIP_CONFLICT`-shaped gap in `lib/core/radio_host/**` — routed to ORCH rather than worked around by editing frozen code.
  2. **Test suites not yet updated** — this is the main remaining work:
     - `test/app_shell/channels_screen_test.dart`, and the Channels/Stations-tab assertions inside `test/app_shell/mobile_app_shell_test.dart`, `test/app_shell/talk_screen_test.dart`, `test/app_shell/shell_harness.dart` all reference retired APIs (`ChannelsScreen`, `ShellKeys.channelsLanding`/`tabChannels`/`tabStations`/`stations`, `legacyFaceRouteName`, `TalkScreen.onSwitchToStations`) and need a genuine rewrite for the Contacts/Groups tabs and real controller wiring (will need a fake/stub `DirectoryClient` HTTP harness, following TASK-090/091's own `FakeDirectoryServer` pattern) — not a mechanical find/replace.
     - `test/features/settings/settings_screen_test.dart` references the removed `SettingsKeys.mode` — needs its mode-row assertions removed/replaced with prefer-direct/messages/identity-action assertions.
     - `test/regression/real_composition_test.dart` (this task's own territory) references `ShellKeys.tabChannels` — needs updating to the new tab set, plus the acceptance criterion's "stubbed directory" composition test.
  3. **Cross-territory breakage discovered, not fixable from here:** `test/regression/regression_shell_harness.dart` and `test/regression/overflow_system_back_test.dart` — **outside this task's `Owned_Paths`** — also reference the retired `ShellKeys.tabChannels`/`tabStations`/`stations` and will fail to compile until updated. Per protocol this is not mine to fix (would be writing outside `Owned_Paths`); flagged here for ORCH to either fold into this task's territory or schedule as a fast-follow once this lands.
  4. Shell-frame and Settings goldens (`test/regression/goldens/goldens/shell_frame_*.png`/`settings_*.png`) are not yet regenerated — blocked on the test-suite rewrite above landing first (goldens must be generated against passing widget trees, not compiled separately).
  5. `flutter build apk --debug` not yet attempted this session.

  **Next session:** (a) build the `FakeDirectoryServer`-backed test harness for Contacts/Groups (mirroring TASK-090/091), (b) rewrite `test/app_shell/mobile_app_shell_test.dart`/`talk_screen_test.dart`/`shell_harness.dart` for Talk/Contacts/Groups, delete `channels_screen_test.dart`, (c) fix `settings_screen_test.dart`'s mode-row references, (d) update `real_composition_test.dart` and regenerate goldens, (e) resolve/escalate gap #1 and #3 to ORCH, (f) full-suite + debug APK evidence before `needs_review`.
