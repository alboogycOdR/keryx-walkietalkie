.pytest_cache/
__pycache__/
*.pyc
.autopilot_state.json
.devteam/
.devteam/atlas.db
.devteam/atlas.db-*
AUTOPILOT_LOG.md
STOP

# Flutter/Dart build and tooling output
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
android/local.properties
*.iml
.idea/

# Per-launch builder runner scripts, prompts and transcripts (dispatch.ps1 detached-window mode).
# Transient: one set per builder session, safe to delete once the window has closed.
.devteam/launch/

# Secrets -- never commit. .env carries the Telegram bot token for
# scripts/release_remote.py (see docs/gofile-telegram-delivery.md).
.env
