/// Retired numbered-channel pending-tune type.
///
/// The class is gone with v1 channel/privacy-code presentation (Technical
/// §6.2). This file stays as an empty library so the out-of-territory
/// `presentation.dart` barrel export remains valid.
library;
