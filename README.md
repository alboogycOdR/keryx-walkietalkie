# Keryx

Android-first, radio-first push-to-talk software.

## Development

```powershell
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```

The Android application ID is `za.co.basileia.keryx` and the minimum Android
SDK is 26.
